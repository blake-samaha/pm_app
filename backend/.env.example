# ===========================================
# PM App Backend - Environment Configuration
# ===========================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# ===========================================

# ----- Database (Required) -----
# For Docker: use db:5432, for local: use localhost:5432
DATABASE_URL=postgresql://postgres:postgres@db:5432/pm_app

# ----- JWT Authentication (Required) -----
# Generate a secure key: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ----- Firebase Authentication (Required) -----
# Get your project ID from Firebase Console → Project Settings
FIREBASE_PROJECT_ID=pm-demo-app

# Path to service account JSON file (Required for token verification)
# Download from Firebase Console → Project Settings → Service accounts → Generate new private key
FIREBASE_SERVICE_ACCOUNT_PATH=/app/firebase-service-account.json

# ----- Jira Integration (OAuth 2.0) -----
# Create an OAuth 2.0 app at: https://developer.atlassian.com/console/myapps/
# Use "OAuth 2.0 (3LO)" authorization, then get client credentials

# Your Atlassian OAuth Client ID
JIRA_CLIENT_ID=your-jira-client-id

# Your Atlassian OAuth Client Secret  
JIRA_CLIENT_SECRET=your-jira-client-secret

# Optional: Your Atlassian site URL (helps identify the correct cloud instance)
JIRA_BASE_URL=https://cognite.atlassian.net

# Optional: Cloud ID (auto-discovered if not set)
# Find at: https://cognite.atlassian.net/_edge/tenant_info
JIRA_CLOUD_ID=

# ----- Salesforce/Precursive Integration (OAuth) -----
# Required for syncing project data and financials from Precursive

# Instance URL (Required if using Precursive)
PRECURSIVE_INSTANCE_URL=https://cognite.lightning.force.com

# OAuth Client Credentials (Connected App)
PRECURSIVE_CLIENT_ID=your-precursive-client-id
PRECURSIVE_CLIENT_SECRET=your-precursive-client-secret

# Leave these blank if using OAuth Client Credentials
PRECURSIVE_USERNAME=
PRECURSIVE_PASSWORD=
PRECURSIVE_SECURITY_TOKEN=

# ----- Environment -----
ENVIRONMENT=development
